{
    "$schema": "https://apify.com/schemas/v1/input.ide.json",
    "title": "PokeAPI Scraper",
    "type": "object",
    "schemaVersion": 1,
    "properties": {
        "pokemon": {
            "title": "Pokémon names or IDs",
            "type": "array",
            "description": "List of Pokémon names or National Pokédex IDs to fetch from PokeAPI's /pokemon/{name|id} endpoint.",
            "editor": "stringList",
            "prefill": ["pikachu", "ditto", "25"]
        }
    },
    "required": ["pokemon"]
}
